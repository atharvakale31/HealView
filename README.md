# HealView

## ClaimsLab

`ClaimsLab.html` is a standalone, offline-capable US healthcare claims data lineage simulator.

Open it directly in a browser:

```bash
open ClaimsLab.html
```

(or double-click the file on desktop environments)
